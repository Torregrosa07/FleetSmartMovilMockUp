package com.fleetsmart.mockupsfleetsmartmovil.ui.theme

import androidx.compose.ui.graphics.Color

object AppColors {
    // Backgrounds
    val Background = Color(0xFFF8FAFC) // Slate-50 (Fondo muy suave)
    val Card = Color(0xFFFFFFFF)

    // Foreground (Textos)
    val Foreground = Color(0xFF0F172A) // Slate-900 (Texto oscuro)
    val CardForeground = Color(0xFF0F172A)

    // Primary (AZUL)
    val Primary = Color(0xFF2563EB) // Blue-600 (Azul vibrante)
    val PrimaryForeground = Color(0xFFFFFFFF) // Texto blanco sobre azul

    // Secondary
    val Secondary = Color(0xFFEFF6FF) // Blue-50 (Azul muy claro para fondos secundarios)
    val SecondaryForeground = Color(0xFF1E40AF) // Blue-800 (Texto azul oscuro)

    // Muted
    val Muted = Color(0xFFF1F5F9) // Slate-100
    val MutedForeground = Color(0xFF64748B) // Slate-500

    // Accent
    val Accent = Color(0xFFDBEAFE) // Blue-100
    val AccentForeground = Color(0xFF1E3A8A)

    // Destructive
    val Destructive = Color(0xFFEF4444) // Red-500
    val DestructiveForeground = Color(0xFFFFFFFF)

    // Border
    val Border = Color(0xFFE2E8F0) // Slate-200

    // Input
    val InputBackground = Color(0xFFFFFFFF)

    // Status Colors
    val Success = Color(0xFF10B981)      // Emerald-500
    val SuccessBackground = Color(0xFFECFDF5)

    val Warning = Color(0xFFF59E0B)      // Amber-500
    val WarningBackground = Color(0xFFFFFBEB)

    val Info = Color(0xFF3B82F6)         // Blue-500
    val InfoBackground = Color(0xFFEFF6FF)
}