package com.fleetsmart.mockupsfleetsmartmovil.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme Colors - Basado en el diseño de escritorio
object AppColors {
    // Backgrounds
    val Background = Color(0xFFFFFFFF)
    val Card = Color(0xFFFFFFFF)

    // Foreground
    val Foreground = Color(0xFF030213)
    val CardForeground = Color(0xFF030213)

    // Primary
    val Primary = Color(0xFF030213)
    val PrimaryForeground = Color(0xFFFFFFFF)

    // Secondary
    val Secondary = Color(0xFFF3F3F5)
    val SecondaryForeground = Color(0xFF030213)

    // Muted
    val Muted = Color(0xFFECECF0)
    val MutedForeground = Color(0xFF717182)

    // Accent
    val Accent = Color(0xFFE9EBEF)
    val AccentForeground = Color(0xFF030213)

    // Destructive
    val Destructive = Color(0xFFD4183D)
    val DestructiveForeground = Color(0xFFFFFFFF)

    // Border
    val Border = Color(0x1A000000) // rgba(0, 0, 0, 0.1)

    // Input
    val InputBackground = Color(0xFFF3F3F5)

    // Success
    val Success = Color(0xFF10B981)
    val SuccessBackground = Color(0xFFECFDF5)

    // Warning
    val Warning = Color(0xFFF59E0B)
    val WarningBackground = Color(0xFFFEF3C7)

    // Info
    val Info = Color(0xFF3B82F6)
    val InfoBackground = Color(0xFFDCEEFE)
}